Vagrant.configure("2") do |config|

  config.vm.box = "debian/jessie64"

  config.vm.network "public_network", bridge: "enp2s0"

  config.vm.provision "shell", inline: "apt install debhelper dh-systemd pkg-config bison libevent-dev libbsd-dev libldns-dev libsqlite3-dev daemon iproute"

end
